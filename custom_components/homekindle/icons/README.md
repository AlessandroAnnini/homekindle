# Petroff weather icons

Matthew Petroff Kindle weather set, CC0.
https://github.com/mpetroff/kindle-weather-display

`svg/` is extracted from `server/weather-script-preprocess.svg` (`<defs>` paths).
PNGs are 64×64 grayscale rasters for Pillow (HAOS has no SVG renderer).
